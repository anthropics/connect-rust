---
# Client conformance test configuration for gRPC protocol
#
# gRPC requires HTTP/2. Supports all RPC types including full-duplex bidi.
# Proto and JSON codecs, identity and gzip compression.

features:
    versions:
        - HTTP_VERSION_2
    protocols:
        - PROTOCOL_GRPC
    codecs:
        - CODEC_PROTO
        - CODEC_JSON
    compressions:
        - COMPRESSION_IDENTITY
        - COMPRESSION_GZIP
        - COMPRESSION_ZSTD
    stream_types:
        - STREAM_TYPE_UNARY
        - STREAM_TYPE_SERVER_STREAM
        - STREAM_TYPE_CLIENT_STREAM
        - STREAM_TYPE_HALF_DUPLEX_BIDI_STREAM
        - STREAM_TYPE_FULL_DUPLEX_BIDI_STREAM
    supports_h2c: true
    supports_tls: true
    supports_tls_client_certs: true
    supports_trailers: true
    supports_connect_get: true
    supports_message_receive_limit: true
    supports_half_duplex_bidi_over_http1: true
